
// use tutorial::Interpreter;
use tutorial::Dog;

fn main() {
    let d = Dog;
    d.bark()
    
}

#[test]
fn basic_test() {
    // let mut i = Interpreter;
    // assert_eq!(i.input("1 + 1"), Ok(Some(2.0)));
}